# IRL QuestBook
This is a simple skeleton for my project, IRL Questbook, which makes your
daily tasks feel like playing a Minecraft modpack.

## Building

This project uses JavaFX and Maven. It requires Java 25+ and Maven to run.
To build, simply navigate to the project root and run the following command:
```$ mvn clean javafx:run```
